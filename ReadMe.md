# Employee Management System

## Author: Menglong Keo
## Advisor: Chayya Chan
## Fields:
- employees (emp_id,first_name (required), last_name (requred), gender (required) , date_of_birth (required), email (required, unique), phone (required, unique), position (required), salary (required), hired_date (required), status (required))
## Feature:
- (List All Employees by pagination, Add New Employee, Update Employee by id, Search Employee (by position, salary, hire_date), Delete Employee by id)
- Employee Report
    - Top Highest Salary by pagination
    - Employee by age
    - KPI by age (>=50, +5%)
    - KPI by hired_date (>10, +5%)
